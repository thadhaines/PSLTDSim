# LTD simulation models / perturbances
# Similar format to dyd files.
# Commented and empty lines are ignored during parsing.
# Percent ramping of load p
load 3 : "pertType" ramp "pertTarget" P "startTime" 2 "RAtime" 6 "RAval" 2 "RAtype" per "holdtime" 6 "RBtime" 4 "RBval" -2 "RBtype" per
